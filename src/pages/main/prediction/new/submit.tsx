import { IonContent,  useIonRouter } from '@ionic/react';

const Submit = () => {
  const router = useIonRouter();
  return (
    <IonContent>
        {/* Games . selection +1 format*/}
    </IonContent>
  );
};

export default Submit;
